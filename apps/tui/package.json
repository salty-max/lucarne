{
  "name": "@lucarne/tui",
  "private": true,
  "description": "Thin wrapper so the Go module joins the turbo pipeline: one `bun run test` still covers every app.",
  "scripts": {
    "start": "go run ./cmd/lucarne",
    "build": "go build -trimpath -ldflags='-s -w' -o dist/lucarne ./cmd/lucarne",
    "typecheck": "go vet ./...",
    "test": "go test ./...",
    "fmt": "gofmt -w ."
  }
}
