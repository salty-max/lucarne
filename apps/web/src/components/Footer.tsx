export function Footer() {
  return (
    <footer className="mt-8 border-t px-4 py-6">
      <div className="mx-auto flex max-w-3xl flex-col gap-1 text-xs text-muted-foreground sm:flex-row sm:items-center sm:justify-between">
        <span className="font-semibold text-foreground">
          Lucarne<span className="text-primary">.</span>
        </span>
        <span>Times in Europe/Paris · broadcasters indicative</span>
      </div>
    </footer>
  );
}
