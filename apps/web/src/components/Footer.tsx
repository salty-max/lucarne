export function Footer() {
  return (
    <footer className="mt-6 border-t px-4 py-6 text-xs text-muted-foreground">
      <div className="mx-auto flex max-w-3xl flex-col gap-1 sm:flex-row sm:items-center sm:justify-between">
        <span>Lucarne — football schedule &amp; broadcasters</span>
        <span>Times in Europe/Paris · broadcasters indicative</span>
      </div>
    </footer>
  );
}
